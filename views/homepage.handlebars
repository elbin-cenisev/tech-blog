<!-- If any posts exist, display them all -->
{{#if posts.length}}
{{#each posts as |post|}}
<div class="post" id="post{{post.id}}">
    <h2>{{post.title}}</h2>
    <h3>Posted by {{post.user.name}} on {{format_time post.created_date}}</h3>
    <p>{{post.text}}</p>
</div>
<hr>
<!-- Display every comment to this post -->
{{#each post.comments as |comment|}}
<div class="comment">
    <p>{{comment.text}}</p>
    <h4>Posted by {{comment.user.name}} on {{format_time comment.created_date}}</h4>
</div>
{{/each}}


{{/each}}

<!-- If no posts have been made, show a generic message -->
{{else}}
<div id="post0" class="post">
    <h2>Nobody has posted anything!</h2>
    <p>Be the first to post something</p>
</div>
{{/if}}